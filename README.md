# ポートフォリオ
Next.js と TailwindCSS で作ったポートフォリオサイトです。
## ローカルホストで実行
下記のコマンドを実行し、`http://localhost:3000`を開いてください。
```
npm run dev
```
## ビルド
GitHubのリモートリポジトリにpushするだけで自動的にビルドされます。
```
git add <ファイル名>
git commit -m <コミットメッセージ>
git push origin main
```